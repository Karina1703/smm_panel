"use client";

import { Popover } from "@headlessui/react";

export const PopoverButton = Popover.Button;
export const PopoverOverlay = Popover.Overlay;
export const PopoverPanel = Popover.Panel;
export const PopoverGroup = Popover.Group;
export { Popover };
